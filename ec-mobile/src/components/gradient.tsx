import { LinearGradient as ELinearGradient, LinearGradientProps } from 'expo-linear-gradient'

export const LinearGradient = (props: LinearGradientProps) => {
  return <ELinearGradient {...props} />
}